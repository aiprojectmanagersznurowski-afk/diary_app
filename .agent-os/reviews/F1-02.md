---
task: F1-02
reviewer: gemini
verdict: approve
date: 2026-09-25 14:56
---
# Recenzja F1-02

## Wynik bramek
Wszystkie bramki jakości przeszły pomyślnie na gałęzi `feat/F1-02-schema-rls`:
- `dod:tsc`: 0 błędów (budżet 0)
- `dod:lint`: OK
- `dod:jest`: OK
- `dod:db-reset`: OK (wszystkie 6 migracji aplikuje się od zera bez błędów)
- `dod:db-test`: OK (wszystkie 44 testy pgTAP w 7 plikach testowych przechodzą — potwierdzone w CI run 36135611930)

## Kryteria akceptacji
- [x] **Wszystkie tabele z §4 istnieją z kolumnami i ograniczeniami jak w dokumentacji:**
  - `profiles` (`supabase/migrations/20260925073142_profiles.sql`)
  - `categories` (`supabase/migrations/20260925073144_categories.sql`)
  - `recordings` (`supabase/migrations/20260925073145_recordings.sql`)
  - `documents` i `document_chunks` (`supabase/migrations/20260925073147_documents.sql`)
  - `links`, `day_rebuild_queue`, `chat_threads`, `chat_messages` (`supabase/migrations/20260925073148_relations_and_chat.sql`)
  - Dowód: `supabase db reset` aplikuje migracje bez błędów na czystej bazie.
- [x] **Każda tabela ma RLS i polityki `user_id = (select auth.uid())`:**
  - Dowód: testy pgTAP w `010_rls_core_tables.test.sql`, `011_rls_documents.test.sql`, `012_rls_relations_and_chat.test.sql` weryfikują pełną izolację SELECT / UPDATE / DELETE między użytkownikami A i B.
- [x] **Indeksy: HNSW na embedding, GIN na fts, btree (user_id, day) i (user_id, kind):**
  - Dowód: `020_indexes.test.sql` sprawdza obecność `document_chunks_embedding_hnsw`, `document_chunks_fts`, `documents_user_day`, `documents_user_kind` (4/4 asercji spełnionych).
- [x] **Buckety prywatne; test pgTAP: A nie może czytać ani zapisać ścieżki {uid_B}/...:**
  - Dowód: `20260925073150_storage.sql` tworzy buckety `recordings` i `documents` z `public = false`. `021_storage.test.sql` testuje brak możliwości zapisu w cudzym folderze (42501 RLS policy violation) i brak dostępu do cudzych plików.
- [x] **`supabase db reset` i `supabase test db` przechodzą:**
  - Dowód: CI run 36135611930 — `· [dod:db-reset] OK`, `· [dod:db-test] OK`.

## Uwagi blokujące
Brak.

## Uwagi nieblokujące
1. W `chat_messages` kolumna `role` nie ma ograniczenia CHECK na dozwolone role (`user`, `assistant` itp.) — zgodne z brakiem ścisłej specyfikacji w `02-architektura.md` §4, można doprecyzować w Fazie 6.
2. Zgodnie z ADR-009 i `02-architektura.md` §8 `user_id` posiada klauzule `ON DELETE CASCADE` do `auth.users(id)` we wszystkich tabelach, co ułatwia realizację prawa do bycia zapomnianym (RODO).

## Zgodność z dokumentacją
- **ADR-001 / ADR-009:** Migracje PostgreSQL w `supabase/migrations/` tworzą pełny schemat docelowy bez zależności od Firebase.
- **`02-architektura.md` §4:** Kolumny, typy danych (`vector(1536)`, `tsvector`), ograniczenia i powiązania w 100% zgodne ze specyfikacją.
- **`02-architektura.md` §8:** RLS włączone na każdej tabeli, zapytania zoptymalizowane pod `(select auth.uid())`.
- **`06-zasady-pracy.md`:** Brak naruszeń bramek jakości, czyste commity w konwencji Conventional Commits.
