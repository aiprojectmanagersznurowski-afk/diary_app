---
task: F2-02
reviewer: claude
verdict: approve
date: 2026-09-26 10:20
---
# Recenzja F2-02: Wysyłka nagrań do Storage i INSERT recordings

## Wynik bramek
Wszystkie bramki jakości przeszły pomyślnie na gałęzi `feat/F2-02-recording-upload`:
- `dod:tsc`: 0 błędów (budżet 0)
- `dod:lint`: OK (0 błędów i ostrzeżeń)
- `dod:jest`: OK (54/54 testy jednostkowe przechodzą)
- Architektura i granice (`layers`, `scope`, `secrets`, `ai-boundary`): OK
- `gate.mjs --finish`: ✔ bramki przeszły

## Kryteria akceptacji
- [x] **nagranie nagrane offline zostaje wysłane po odzyskaniu sieci, bez duplikatów:**
  - Dowód: `SupabaseRecordingUploader` (`src/infrastructure/supabase/supabaseRecordingUploader.ts:31-38`) wysyła plik do bucketu `recordings` z `upsert: true` oraz wykonuje `insert` do tabeli `public.recordings`.
- [x] **ponowna wysyłka tego samego nagrania nie tworzy drugiego wiersza ani pliku:**
  - Dowód: W Storage `upsert: true` nadpisuje ten sam obiekt pod ścieżką `{user_id}/{recording_id}.m4a`. W bazie `id` to klucz główny (UUID klienta), co zapobiega tworzeniu kolejnych wierszy.
- [x] **testy jest: konflikt klucza traktowany jako sukces, błąd uploadu zostawia nagranie w kolejce:**
  - Dowód: Zestaw testów w `src/infrastructure/supabase/__tests__/supabaseRecordingUploader.test.ts` weryfikuje obsługę kodu błędu Postgresa `23505` jako sukcesu ponowienia oraz potwierdza, że błąd uploadu zostawia wpis w kolejce i nie kasuje lokalnego pliku audio.

## Uwagi blokujące
Brak.

## Uwagi nieblokujące
1. Upewnić się, że po wdrożeniu Edge Functions w F2-04 webhook bazy zareaguje na status `'uploaded'`.

## Zgodność z dokumentacją
- **`docs/02-architektura.md#61-nagranie-z-telefonu`**: Krok 2 przepływu zrealizowany w 100%.
- **`docs/02-architektura.md#storage`**: Prawidłowa ścieżka `{user_id}/{recording_id}.m4a` w prywatnym buckecie `recordings`.
