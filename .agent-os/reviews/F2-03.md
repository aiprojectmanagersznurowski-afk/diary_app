---
task: F2-03
reviewer: claude
verdict: approve
date: 2026-09-26 10:31
---
# Recenzja F2-03: Warstwa _shared/ai z adapterami Groq i Gemini

## Wynik bramek
Wszystkie bramki jakości przeszły pomyślnie na gałęzi `feat/F2-03-shared-ai`:
- `dod:tsc`: 0 błędów (budżet 0)
- `dod:lint`: OK (0 błędów i ostrzeżeń)
- `dod:jest`: OK (54/54 testy jednostkowe przechodzą)
- `dod:deno-test`: OK (16/16 testów przechodzi)
- Architektura i granice (`layers`, `scope`, `secrets`, `ai-boundary`): OK
- `gate.mjs --finish`: ✔ bramki przeszły

## Kryteria akceptacji
- [x] **zmiana dostawcy/modelu zadania wymaga tylko zmiany zmiennej środowiskowej:**
  - Dowód: `aiConfig.ts` mapuje zmienne środowiskowe (`STT_*`, `LLM_STRUCTURE_*`, `LLM_DIGEST_*`, `LLM_LINK_*`, `LLM_CHAT_*`, `EMBED_*`) na konfigurację adapterów w fabryce `createAiProviders` (`factory.ts`). Test `factory_test.ts` weryfikuje nadpisywanie dostawców i modeli przez zmienne środowiskowe.
- [x] **embeddingi mają długość EMBED_DIM (1536); inna długość = błąd:**
  - Dowód: `GeminiEmbeddingAdapter` weryfikuje długość każdego wektora (`vector.length !== this.dimension`) i rzuca wyjątek przy jakiejkolwiek niezgodności. Zweryfikowane w teście `GeminiEmbeddingAdapter - rzuca błąd gdy długość wektora różni się od EMBED_DIM`.
- [x] **deno test: każdy adapter z atrapą fetch (sukces, 429 z ponowieniem, błąd), bez prawdziwych kluczy:**
  - Dowód: Zestaw 16 testów w `groq_test.ts`, `gemini_test.ts`, `retry_test.ts`, `factory_test.ts` testuje wszystkie scenariusze używając atrap funkcji fetch bez wykonywania zapytań do zewnętrznej sieci i bez prawdziwych kluczy.
- [x] **żaden plik poza _shared/ai nie woła dostawców (bramka ai-boundary):**
  - Dowód: Bramka `ai-boundary` weryfikuje brak zewnętrznych SDK (`groq-sdk`, `@google/genai`, `openai`) w kodzie klienckim oraz zakazuje domen dostawców poza katalogiem `supabase/functions/_shared/ai/**`. Bramka przeszła pomyślnie.

## Uwagi blokujące
Brak.

## Uwagi nieblokujące
1. Pamiętać o ustawieniu sekretów na środowisku docelowym Supabase: `CP-SECRETS`.
