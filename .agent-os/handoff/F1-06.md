---
task: F1-06
implementer: gemini
status: review   # in_progress | blocked | review | done
branch: build/F1-06-remove-firebase
updated: 2026-09-25 15:52
---
# Handoff F1-06: Usunięcie Firebase i martwych zależności

Plik przekazania pracy między sesjami i agentami (Claude ↔ Gemini). Aktualizuj go na koniec
każdej sesji (workflow /os-handoff). Następny agent zaczyna od przeczytania tego pliku.

## Stan

Całkowite usunięcie Firebase i martwych zależności zostało zakończone i zweryfikowane:
- **Pakiety npm (`package.json`, `package-lock.json`)**:
  - Odinstalowano wszystkie pakiety Firebase: `@react-native-firebase/app`, `@react-native-firebase/auth`, `@react-native-firebase/firestore`.
  - Odinstalowano martwe pakiety: `expo-av`, `drizzle-orm`, `drizzle-kit`, `expo-build-properties`.
  - Zaktualizowano zależności Expo do wersji w pełni spójnych z SDK 54 (`npx expo install --check`, `npm dedupe`).
- **Pliki konfiguracyjne i natywne**:
  - Usunięto pliki `google-services.json`, `GoogleService-Info.plist`, `test-metering.js` oraz katalog `src/infrastructure/firebase/`.
  - W `app.json` usunięto referencje `googleServicesFile`, wtyczki Firebase, `expo-av` oraz `expo-build-properties`.
  - Skonfigurowano uprawnienia mikrofonu bezpośrednio w wtyczce `expo-audio`.
  - Poprawiono referencje do ikon w `app.json` na prawidłowe pliki PNG, eliminując błędy walidacji schematu.
- **Kod źródłowy**:
  - `src/infrastructure/db/diaryRepository.ts`: zastąpiono `FirestoreDiaryRepository` implementacją `InMemoryDiaryRepository` (bez żadnych zależności od Firebase).
  - `src/application/useCases/auth/__tests__/authUseCases.test.ts`: usunięto przestarzałe mocki Firebase.
  - Zaktualizowano dokumentację warstw w `src/application/README.md`, `src/infrastructure/README.md`, `src/presentation/README.md` zgodnie z ADR-009.
- **Narzędzia i testy**:
  - `npx expo-doctor`: 18/18 checks passed, brak jakichkolwiek problemów.
  - `jest`: 24/24 testy jednostkowe przechodzą pomyślnie.
  - `tsc --noEmit`: 0 błędów (budżet: 0).
  - `eslint`: 0 błędów i ostrzeżeń.

## Następny krok
1. Wykonanie recenzji krzyżowej przez agenta `claude` (`.agent-os/reviews/F1-06.md` z werdyktem `approve`).
2. Scalenie gałęzi `build/F1-06-remove-firebase` do `main`.
3. Przejście do zadania F1-07 (`Unieważnienie obecnego klucza Groq; klucze przeniesione do sekretów Supabase`).

## Kryteria akceptacji
- [x] w kodzie i package.json nie ma Firebase ani expo-av, drizzle-orm, drizzle-kit — zweryfikowane pełnym przeszukaniem repozytorium
- [x] npx expo-doctor bez błędów; dev client buduje się na iOS i Androidzie — wynik expo-doctor: 18/18 passed
- [x] tsc i lint w budżecie — 0 błędów TypeScript i 0 błędów ESLint

## Decyzje w trakcie
- **Usunięcie expo-build-properties:** Wtyczka była wykorzystywana wyłącznie do `forceStaticLinking` pakietów Firebase. Po ich usunięciu wtyczka stała się zbędna i została bezpiecznie usunięta.
- **Tymczasowe InMemoryDiaryRepository:** Zapewnia działanie mockowanego zapisu wpisów bez zależności zewnętrznych do czasu wdrożenia pełnego przetwarzania w Supabase w fazie 2.
- **Aktualizacja README warstw (ADR-009):** Stare instrukcje w `src/*/README.md` odwołujące się do Firebase i expo-av zastąpiono zwięzłą dokumentacją Clean Architecture.

## Sprawdzone ręcznie
- `npx -y expo-doctor`: 18/18 checks passed. No issues detected!
- `npx jest`: 24/24 pass.
- `npx tsc --noEmit`: 0 błędów.
- `npx eslint . --max-warnings=0`: 0 błędów.
- `node .agent-os/scripts/gate.mjs --finish`: wszystkie bramki przechodzą.

## Zmienione dokumenty
- `docs/04-roadmapa.md` — odhaczono zadanie F1-06.

## Checkpointy
- **CP-DEVICE**:
  - Pełny build dev client po usunięciu natywnych bibliotek Firebase i expo-av (czyste `npx expo prebuild --clean` lub EAS Build).

## Czeka na człowieka
- Przebudowanie dev clienta (CP-DEVICE) na urządzeniu/symulatorze.

## Sesje
- 2026-09-25 15:52 gemini: usunięcie Firebase, expo-av, drizzle, aktualizacja app.json, diaryRepository, testów, README warstw, expo-doctor 18/18 pass, gotowe do recenzji
- 2026-09-25 15:45 gemini: start
