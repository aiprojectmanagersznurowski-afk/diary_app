---
task: F1-08
implementer: claude
status: review   # in_progress | blocked | review | done
branch: build/F1-08-lint-typecheck
updated: 2026-09-24 18:17
---
# Handoff F1-08: ESLint, tsconfig bez design_exports, tsc bez błędów

Plik przekazania pracy między sesjami i agentami (Claude ↔ Gemini). Aktualizuj go na koniec
każdej sesji (workflow /os-handoff). Następny agent zaczyna od przeczytania tego pliku.

## Stan
Zadanie ukończone, gotowe do recenzji drugiego agenta. Jeden commit: `6da3b43`.

Po drodze znalazłem i naprawiłem dodatkowy problem: `package.json` nigdy nie miał konfiguracji
Prettier, więc `lint-staged`'owe `prettier --write` reformatowało CAŁY dotknięty plik do domyślnego
stylu Prettier (podwójne cudzysłowy, wąskie łamanie linii) zamiast stylu już używanego w repo
(pojedyncze cudzysłowy). To samo w sobie nieszkodliwe, ale przy pierwszym commicie ubocznie
"dotykało" linie importów, których nie zmieniałem semantycznie (np. w `OnboardingScreen.tsx`,
`SettingsScreen.tsx`), co fałszywie uruchomiło bramkę `layers` na starym długu architektonicznym
spoza zakresu tego zadania (właściwy cel: F1-09). Naprawa: dodałem `"prettier": {"singleQuote":
true, "printWidth": 120}` do `package.json` (w zakresie `scope.write`; osobny `.prettierrc.json`
nie jest, próbowałem najpierw tak i bramka `scope` to słusznie odrzuciła), zresetowałem pierwszy
commit i scaliłem wszystko w jeden, żeby diff względem `main` był czysty i minimalny.

- Zainstalowano `eslint` + `eslint-config-expo` (flat config, `eslint.config.js`) i `jest` + `jest-expo`
  (`jest.config.js`) przez `npx expo install ... -- --save-dev` (dopasowanie do SDK 54).
- `tsconfig.json`: dodano `"exclude": ["node_modules", "design_exports"]`.
- Naprawiono realne błędy (nie tylko uciszono lint):
  - `expoAudioRecorder.ts`: zła nazwa zdarzenia `'RECORDING_STATUS_UPDATE'` → `'recordingStatusUpdate'`
    (zgodna z typami `expo-audio`), usunięty zdublowany martwy `return null`. `AudioModule.AudioRecorder`
    zostało (to jedyny udokumentowany sposób tworzenia rekordera poza hookiem `useAudioRecorder`),
    dodany `eslint-disable-next-line import/namespace` z komentarzem — sprawdziłem w
    `node_modules/expo-audio/build/AudioModule.types.d.ts`, że to nie jest błąd typów, tylko fałszywy
    alarm reguły `import/namespace` na re-eksporcie typu przez barrel `index.d.ts` (`export type *`).
  - `DetailScreen.tsx`: `pData` nie miało pola `emotionTriggers` → wyzwalacze emocji nigdy się nie
    renderowały (audyt #6). Dodano `emotionTriggers: parsedData.emotionTriggers || []`.
  - `App.tsx`: efekt logowania miał `[initializing]` w zależnościach, więc `setInitializing(false)`
    wewnątrz callbacku re-subskrybował listener i podwajał `syncGoalsFromCloud`/`syncFromCloud` przy
    logowaniu (audyt #11). Deps na `[]`, jednorazowe wywołania przez `useXStore.getState()`.
  - `UIPrimitives.tsx`: `GlassCard`/`GradientText` miały `style?: ViewStyle | ViewStyle[]` zamiast
    `StyleProp<T>` — to był rzeczywisty powód 3 błędów tsc w `BadgesScreen`/`SettingsScreen`
    (tablica stylu z warunkowym `false` nie mieściła się w wąskim typie). Też zawężono `colors` w
    `GradientTextProps` do `readonly [string, string, ...string[]]`, bo tego wymaga `LinearGradient`
    — sprawdziłem wszystkie wywołania `<GradientText colors={...}>` w repo, wszystkie przekazują
    literały ≥2-elementowe, więc bez ryzyka.
- Reszta to usunięcie faktycznie nieużywanych importów (`Ionicons`, `width`, `LinearGradient` × 2,
  `RecordButton`, `Alert`, `firebase` default) i eskejpowanie `"` w `DetailScreen.tsx` (JSX).
- `eslint.config.js` wyklucza `.agent-os/**`, `.agents/**`, `.claude/**`, `.gemini/**`, `.husky/**`
  (poza zakresem tego zadania, nie mam tam prawa zapisu) i `test-metering.js` (martwy skrypt spoza
  `scope.write`, zostawiony bez zmian).
- `git commit` przeszedł przez `pre-commit` (lint-staged: `eslint --fix` + `prettier --write` —
  przeformatował cudzysłowy na `"` i złamał kilka długich linii; bez zmiany logiki), potwierdzając
  kryterium akceptacji o pre-commit.

## Następny krok
Recenzja drugiego agenta (`/os-review F1-08`), potem człowiek otwiera PR z `build/F1-08-lint-typecheck`
do `main` i po scaleniu wykonuje CP-GATE (patrz niżej).

## Kryteria akceptacji
- [x] `npx tsc --noEmit`: 0 błędów
- [x] `npx eslint . --max-warnings=0` przechodzi
- [x] commit z plikiem `.ts` przechodzi przez pre-commit (lint-staged + bramki) — zweryfikowane realnym commitem `6da3b43`
- [x] `npx jest --ci --passWithNoTests` działa (jest-expo skonfigurowany)

## Decyzje w trakcie
- `AudioModule.AudioRecorder` zostaje zamiast bezpośredniego `new AudioRecorder(...)`: ten drugi
  wygląda kusząco (klasa jest zaimportowana i użyta jako typ pola), ale pakiet eksportuje ją z
  `index.d.ts` jako `export type *`, więc nie da się jej użyć jako wartości-konstruktora poza
  `AudioModule`. Próbowałem, tsc: „cannot be used as a value because it was exported using
  'export type'”. Zostawiłem oryginalne API z eslint-disable i komentarzem, żeby następna osoba
  nie próbowała tego samego.
- Nie ruszałem `firebaseConfig.ts` poza usunięciem nieużywanego importu `firebase` — plik i tak
  znika w F1-06 (usunięcie Firebase), więc żadnych innych zmian tu, zgodnie z „nie refaktoruj przy
  okazji”.
- `eslint-config-expo` i `eslint` wylądowały w wersjach z rejestru npm w dniu instalacji
  (`~10.0.0` / `^9.39.5`) przez `npx expo install`; nie przypinałem ręcznie.

## Sprawdzone ręcznie
Nie dotyczy — zadanie to wyłącznie tooling (ESLint/tsc/jest) i poprawki typów bez zmiany zachowania
w wątkach nagrywania/logowania poza tym, co opisane wyżej (usunięcie podwójnej synchronizacji przy
logowaniu i literalna poprawka nazwy zdarzenia audio na zgodną z API). CP-DEVICE zgłoszony niżej,
bo dotyka `expoAudioRecorder.ts` i `App.tsx` (logowanie) — realny test na urządzeniu polecam przed
scaleniem albo zaraz po, patrz scenariusz.

## Zmienione dokumenty
- `docs/04-roadmapa.md`: odhaczona pozycja F1-08; z tabeli „Znane błędy” usunięte 5 wierszy
  naprawionych w tym zadaniu (`DetailScreen.tsx` emotionTriggers, `expoAudioRecorder.ts`,
  `App.tsx` podwójny nasłuch, `package.json` ESLint, `tsconfig.json` design_exports), z notką
  które to były.

## Checkpointy
- **CP-DEP**: nowe zależności (devDependencies): `eslint-config-expo`, `eslint`, `jest`, `jest-expo`.
  Uzasadnienie: wymagane wprost przez kontrakt F1-08 i `docs/03-stos-technologiczny.md`
  (ESLint zastępuje niedziałający `lint-staged`, jest-expo to oficjalny preset testowy Expo).
  Alternatyw nie szukałem — to jest infrastruktura wskazana w dokumentacji, nie wybór z kilku opcji.
  Licencje: obie MIT, standardowe dla ekosystemu Expo/RN.
- **CP-DEVICE**: `expoAudioRecorder.ts` (poprawka nazwy zdarzenia audio) i `App.tsx` (zmiana
  cyklu życia nasłuchu logowania) wymagają testu na prawdziwym urządzeniu, nie tylko `tsc`/`eslint`.
  Scenariusz: (1) zbuduj dev client (`npx expo run:ios` / `run:android`), (2) zaloguj się i sprawdź
  w logach, że synchronizacja z chmury (`syncGoalsFromCloud`/`syncFromCloud`) uruchamia się **raz**,
  nie dwa razy, (3) nagraj krótką notatkę głosową i sprawdź w `RecordingOverlay`, że wizualizacja
  poziomu głośności nadal reaguje na głos (to zależy od `addListener('recordingStatusUpdate', ...)`,
  które teraz faktycznie nasłuchuje właściwego zdarzenia — wcześniej nasłuchiwało nieistniejącego
  `'RECORDING_STATUS_UPDATE'`, więc ten listener nigdy nie strzelał; sama metryka i tak działa przez
  polling w `RecordingOverlay`, ale warto potwierdzić, że nic się nie zepsuło).
- **CP-GATE**: po scaleniu tego PR do `main` człowiek ustawia w `.agent-os/contracts/gates.yaml`
  `dod.tsc.error_budget` z 204 na **0** (204 to wartość tymczasowa, którą ustawiłem 2026-09-24,
  zanim jeszcze istniał ten task — była źle skalibrowana na 13 i liczona bez `design_exports/`;
  teraz po wykluczeniu `design_exports/` i wyzerowaniu błędów realna liczba to 0, więc budżet
  można od razu zaostrzyć).

## Czeka na człowieka
- Scalenie PR (po recenzji drugiego agenta).
- CP-DEP: zatwierdzenie nowych zależności (wyżej).
- CP-DEVICE: test na urządzeniu (scenariusz wyżej) — może być równolegle z recenzją, nie blokuje jej.
- CP-GATE: zaostrzenie budżetu tsc do 0 po scaleniu.

## Sesje
- 2026-09-24 18:17 claude: F1-08 gotowe do recenzji
- 2026-09-24 17:44 claude: start
- 2026-09-24 18:20 claude: zadanie ukończone, status → review
