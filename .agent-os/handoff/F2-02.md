---
task: F2-02
implementer: gemini
status: review
branch: feat/F2-02-recording-upload
updated: 2026-09-26 10:15
---
# Handoff F2-02: Wysyłka nagrań do Storage i INSERT recordings

Plik przekazania pracy między sesjami i agentami (Claude ↔ Gemini). Aktualizuj go na koniec
każdej sesji (workflow /os-handoff). Następny agent zaczyna od przeczytania tego pliku.

## Stan
- Zaimplementowano `SupabaseRecordingUploader` w `src/infrastructure/supabase/supabaseRecordingUploader.ts` implementujący `IRecordingUploader`.
- Zintegrowano pobieranie identyfikatora użytkownika (`supabase.auth.getUser()`) spełniające polityki RLS Storage i bazy.
- Zaimplementowano upload audio do bucketu `recordings` pod ścieżkę `{user_id}/{recording_id}.m4a` z flagą `upsert: true`.
- Zaimplementowano `insert` do tabeli `public.recordings` z kluczem głównym klienta (`recording.id`) oraz statusem `'uploaded'`.
- Zaimplementowano obsługę idempotencji: konflikt klucza głównego (kod błędu PostgreSQL `23505` / `duplicate key`) jest traktowany jako sukces ponowienia.
- Zaktualizowano Composition Root w `src/composition/diary.ts` – `SupabaseRecordingUploader` zastąpił tymczasową atrapę `MockRecordingUploader`.
- Pokryto testami jednostkowymi w Jest (`src/infrastructure/supabase/__tests__/supabaseRecordingUploader.test.ts`): wszystkie 54 testy w repozytorium przechodzą.
- TypeScript (`npx tsc --noEmit`) 0 błędów, ESLint 0 błędów/ostrzeżeń.

## Następny krok
Recenzja krzyżowa /os-review, merge do `main` i przejście do zadania F2-03 (`_shared/ai`: interfejsy i adaptery Groq i Gemini).

## Kryteria akceptacji
- [x] nagranie nagrane offline zostaje wysłane po odzyskaniu sieci, bez duplikatów
- [x] ponowna wysyłka tego samego nagrania nie tworzy drugiego wiersza ani pliku
- [x] testy jest: konflikt klucza traktowany jako sukces, błąd uploadu zostawia nagranie w kolejce

## Decyzje w trakcie
- `SupabaseRecordingUploader` przyjmuje wstrzykiwany `fileReader` (domyślnie czyta przez Expo SDK 54 `File.bytes()` lub `FormData`), co zapewnia pełną testowalność jednostkową.
- `upsert: true` w wywołaniu Supabase Storage oraz przechwytywanie błędu Postgresa `23505` zapewniają bezpieczną idempotencję przy ponawianiu.

## Sprawdzone ręcznie
- Testy jednostkowe w `supabaseRecordingUploader.test.ts` weryfikują:
  - sukces uploadu do Storage i insertu do tabeli `recordings`,
  - obsłużenie konfliktu klucza `23505` jako sukcesu,
  - błąd uploadu skutkujący pozostawieniem nagrania w kolejce offline i zachowaniem pliku na dysku.

## Zmienione dokumenty
- `docs/04-roadmapa.md` (odhaczono zadanie F2-02)

## Checkpointy
- CP-DEVICE: Scenariusz testu na urządzeniu:
  1. Nagranie notatki w trybie samolotowym (offline).
  2. Po wyłączeniu trybu samolotowego (powrót sieci) aplikacja automatycznie przesyła plik do Supabase Storage i rejestruje wpis w tabeli `recordings` ze statusem `uploaded`.

## Czeka na człowieka
- Wykonanie CP-DEVICE przy okazji testowania na telefonie.

## Sesje
- 2026-09-26 10:10 gemini: start i implementacja zadania F2-02 (SupabaseRecordingUploader, idempotencja, testy)
