---
task: F1-07
implementer: claude
status: review
branch: chore/F1-07-rotate-ai-keys
updated: 2026-09-25 15:56
---
# Handoff F1-07: Unieważnienie klucza Groq i przeniesienie kluczy do sekretów Supabase

## Stan
Zaktualizowano pliki `.env.example` oraz `supabase/config.toml` pod kątem sekretów Edge Functions (`GROQ_API_KEY`, `GEMINI_API_KEY`), bez prefiksu `EXPO_PUBLIC_`. Zadanie przygotowuje projekt pod architekturę Edge Functions (ADR-003, Faza 2). Dodano precyzyjną instrukcję postępowania w ramach checkpointu `CP-SECRETS`. Odhaczono zadanie w `docs/04-roadmapa.md`. Bramki tsc, lint i testy przechodzą bez błędów.

## Następny krok
Przeprowadzenie recenzji krzyżowej (`.agent-os/reviews/F1-07.md`), weryfikacja bramki PR (`gate.mjs --pr`), utworzenie i scalenie PR do gałęzi `main`, a następnie wykonanie przez człowieka kroków `CP-SECRETS`.

## Kryteria akceptacji
- [x] .env.example opisuje GROQ_API_KEY i GEMINI_API_KEY jako sekrety Supabase (bez EXPO_PUBLIC_)
- [x] handoff zawiera listę kroków człowieka z CP-SECRETS

## Decyzje w trakcie
Zgodnie z kontekstem kontraktu zadania F1-07 agent nie ma dostępu i nigdy nie czyta pliku `.env`. Klucze są konfigurowane wyłącznie przez człowieka w konsoli i CLI Supabase.

## Sprawdzone ręcznie
Nie dotyczy — zmiana konfiguracji szablonu zmiennych środowiskowych i dokumentacji.

## Zmienione dokumenty
docs/04-roadmapa.md

## Checkpointy
CP-SECRETS:
- Unieważnić bieżący klucz Groq w konsoli Groq (https://console.groq.com/keys) i wygenerować nowy klucz.
- Ustawić sekrety na serwerze Supabase za pomocą CLI: `supabase secrets set GROQ_API_KEY=... GEMINI_API_KEY=...`
- Usunąć `EXPO_PUBLIC_GROQ_API_KEY` oraz `GOGLE_API` ze swojego lokalnego `.env`.

## Czeka na człowieka
CP-SECRETS — unieważnienie i wygenerowanie nowego klucza Groq, ustawienie sekretów w Supabase i oczyszczenie lokalnego .env.

## Sesje
- 2026-09-25 15:53 claude: start
- 2026-09-25 15:56 claude: konfiguracja .env.example, supabase/config.toml, aktualizacja roadmapy i handoff
