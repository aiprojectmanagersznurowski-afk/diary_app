---
task: F1-01
implementer: claude
status: review   # in_progress | blocked | review | done
branch: build/F1-01-supabase-init
updated: 2026-09-24 23:30
---
# Handoff F1-01: Projekt Supabase i lokalne środowisko w repozytorium

Plik przekazania pracy między sesjami i agentami (Claude ↔ Gemini). Aktualizuj go na koniec
każdej sesji (workflow /os-handoff). Następny agent zaczyna od przeczytania tego pliku.

## Stan
Zadanie ukończone i **realnie zweryfikowane** — patrz „Sprawdzone ręcznie" (nie na moim laptopie,
tylko przez CI, co jest tu opisane wprost, żeby nie było wątpliwości co faktycznie się stało).

- `supabase init` (Supabase CLI 2.117.0, zainstalowane lokalnie przez `brew install supabase/tap/supabase`,
  bo go nie było): `supabase/config.toml`, `supabase/.gitignore`.
- `supabase/migrations/20260924205321_enable_extensions.sql`: `vector`, `pg_cron`, `pg_net`, `unaccent`
  do schematu `extensions`, konwencja potwierdzona przez dokumentację Supabase (WebFetch:
  `create extension pgtap with schema extensions;` z `docs/guides/database/extensions`).
- `supabase/tests/database/extensions.test.sql`: pgTAP, `has_extension()` dla wszystkich czterech,
  konwencja lokalizacji/nazewnictwa i boilerplate z oficjalnej dokumentacji Supabase (WebFetch:
  `docs/guides/local-development/testing/overview`).
- `supabase/seed.sql`: pusty plik z komentarzem — `config.toml` (`db.seed.sql_paths`) go referencjonuje.
- `config.toml`: dodane `[auth.external.google]` (nie było w szablonie CLI) i uzupełniony
  `[auth.external.apple].client_id` — oba dostawcy `enabled = false`, `client_id`/`secret`
  wskazują na `env(SUPABASE_AUTH_EXTERNAL_<PROVIDER>_<CLIENT_ID|SECRET>)`, bez wpisanych wartości.
- Po drodze naprawiony osobny błąd kitu (PR #2, scalony): `supabase/.gitignore` brakowało na liście
  `migrations.pr_companions` w `.agent-os/contracts/gates.yaml`, więc każde zadanie inicjujące
  Supabase razem z pierwszą migracją musiało oblać bramkę `migrations` niezależnie od treści.

## Następny krok
Recenzja drugiego agenta (`/os-review F1-01`), potem człowiek: przegląd SQL (CP-MIGRATION) i
`supabase db push` po utworzeniu projektu w chmurze (CP-SECRETS, jeśli jeszcze nie istnieje).

## Kryteria akceptacji
- [x] `supabase start` i `supabase db reset` działają od zera na czystym klonie — **potwierdzone
      przez CI**, nie lokalnie (dowód niżej)
- [x] migracja włącza rozszerzenia vector, pg_cron, pg_net, unaccent
- [x] config.toml: Auth z dostawcami google i apple przez env(...), bez wpisanych sekretów
- [x] test pgTAP w supabase/tests/ sprawdza obecność rozszerzeń — **potwierdzone przez CI**

## Decyzje w trakcie
- Człowiek świadomie zrezygnował z lokalnego Dockera/Colimy na tej maszynie na rzecz podejścia
  „w pełni w chmurze". Efekt: nie miałem żadnego sposobu na self-review SQL przed pokazaniem go
  do recenzji na moim laptopie. Zamiast zgadywać, otworzyłem PR jako **draft** wyłącznie po to,
  żeby CI (który ma własnego Dockera na maszynie GitHuba — nie to samo, co lokalny Docker,
  którego człowiek nie chciał u siebie) uruchomił `supabase start`/`db reset`/`test db` naprawdę.
  Zadziałało za pierwszym razem — migracja i test są poprawne.
- Nie ustawiłem `enabled = true` dla dostawców Auth w `config.toml` — realne sekrety i włączenie
  to F1-03. Skoro `dod:db-reset` na CI przeszedł z `enabled = false` i env()-ami bez wartości,
  potwierdza to też, że ta decyzja nie blokowała startu Auth.
- Schemat `extensions` dla wszystkich czterech rozszerzeń: potwierdzony w dokumentacji dla ogólnej
  konwencji (przykład na `pgtap`), przyjęty przez analogię dla reszty — i teraz też potwierdzony
  empirycznie przez zielony `dod:db-reset`.

## Sprawdzone ręcznie
**Nie na moim laptopie — nie mam tam Dockera ani Colimy (świadoma decyzja człowieka).** Zamiast
tego: otworzyłem PR #3 jako draft z tej gałęzi, co uruchomiło CI (`agent-os.yml`, job `gates`,
który ma Docker na maszynie GitHuba). Wynik, run
[36061369891](https://github.com/aiprojectmanagersznurowski-afk/diary_app/actions/runs/36061369891):

```
· [dod:db-reset] OK
· [dod:db-test] OK
```

To jest realne uruchomienie `supabase db reset` (od zera, na czystym klonie tej gałęzi) i
`supabase test db` (pgTAP), nie symulacja. Pozostałe błędy w tym samym przebiegu CI
(`roadmap`, `review`, `handoff status`) były oczekiwane — PR był draftem właśnie po to, żeby
dostać ten wynik przed formalnym zgłoszeniem do recenzji; ten commit je domyka.

## Zmienione dokumenty
- `docs/04-roadmapa.md`: odhaczona pozycja F1-01 (tym razem zasadnie — wcześniej cofnięta,
  bo była przedwczesna, teraz kryteria są realnie potwierdzone).

## Checkpointy
- **CP-MIGRATION**: pierwsza migracja repozytorium. `supabase db reset`/`test db` już potwierdzone
  przez CI — człowiek i tak przegląda SQL przed `supabase db push` na projekt zdalny.
- **CP-SECRETS**: nie dotyczy jeszcze bezpośrednio (żadne wartości nie są tu ustawiane), ale
  `config.toml` zakłada istnienie zmiennych `SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID`,
  `SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET`, `SUPABASE_AUTH_EXTERNAL_APPLE_CLIENT_ID`,
  `SUPABASE_AUTH_EXTERNAL_APPLE_SECRET` — realne wartości i włączenie (`enabled = true`) to F1-03.

## Czeka na człowieka
- Scalenie PR (po recenzji drugiego agenta) — trzeba oznaczyć PR #3 jako "Ready for review"
  (dziś jest draft).
- CP-MIGRATION: przegląd SQL i `supabase db push` po utworzeniu projektu w chmurze (CP-SECRETS).

## Sesje
- 2026-09-24 23:30 claude: F1-01 potwierdzone przez CI (dod:db-reset/db-test OK), gotowe do recenzji
- 2026-09-24 22:51 claude: start
- 2026-09-24 23:10 claude: napisany config/migracja/test bez lokalnej weryfikacji (decyzja: bez
  Dockera/Colimy); status → blocked, czeka na realne uruchomienie
- 2026-09-24 23:35 claude: CI (draft PR #3) potwierdziło `db-reset`/`test db` OK; status → review
